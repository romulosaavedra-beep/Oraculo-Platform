# Variáveis de ambiente para o Backend do Oraculo

# Configuração do Supabase
# A URL do seu projeto Supabase
SUPABASE_URL="your_supabase_url"
# A chave de SERVIÇO (service_role) do seu projeto Supabase. Mantenha em segredo.
SUPABASE_SERVICE_KEY="your_supabase_service_key"
# O segredo JWT do seu projeto Supabase (encontrado em Configurações > API)
SUPABASE_JWT_SECRET="your_supabase_jwt_secret"

# Configuração da API do Google Gemini
# Sua chave de API para o Google Gemini
ORACULO_GEMINI_API_KEY="your_gemini_api_key"

# Configuração de Segurança do FastAPI (usado para tokens JWT próprios, se aplicável)
# Um segredo forte e aleatório. Você pode gerar um com: openssl rand -hex 32
SECRET_KEY="a_strong_random_secret_key"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Configuração do Redis e Celery para tarefas em background
# A URL de conexão para o seu servidor Redis
REDIS_URL="redis://localhost:6379/0"
CELERY_BROKER_URL="redis://localhost:6379/0"
CELERY_RESULT_BACKEND="redis://localhost:6379/0"

# Configuração do Banco de Dados (usado pelo listener de notificações do Supabase)
# A string de conexão do seu banco de dados PostgreSQL no Supabase
DATABASE_URL="postgresql://user:password@host:port/dbname"

# Configuração da Aplicação
PROJECT_NAME="Oraculo API"
API_V1_STR="/api/v1"
